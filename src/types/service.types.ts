export interface IGetServicesStatisticsBody {
    code: string;
}

export interface IGetServiceByIdBody {
    service_id: string;
}

export interface IGetWorkerByServiceParam {
    service_id: string;
}